# ❄️ «Северное сияние» — общий план

Единый рабочий документ проекта: что задумано, что уже сделано (сверено с кодом),
что осталось. Обновляется вместе с кодом — это источник правды, а не переписка.

- **Репозиторий:** `bxbyarchi/luna`
- **Дата сверки с кодом:** 21.09.2026
- **Статус:** фундамент собран, production запущен, идёт стабилизация API/БД

---

## 0. Два направления — не смешивать

| | `luna` (этот репозиторий) | «Северное сияние» |
|---|---|---|
| Что это | складская / управленческая система | полная система ярмарки |
| Состав | склады, товары, перемещения, сотрудники | склады + площадки + домики + кассы + кассиры + смены + продажи |
| Статус | production на Render | этап 4+, ещё не начат |

Бренд «Северное сияние» уже используется в интерфейсе. Название «Луна» в ярмарочную
часть не добавляется.

---

## 1. Главная идея

Единая система управления складами, товарами, перемещениями, сотрудниками, а в
дальнейшем — ярмарками и кассами.

Архитектура обязана выдержать рост с 5 площадок до 30–40 домиков и касс **без
переписывания**. Поэтому сущности разделены принципиально:

```
склады ≠ площадки ≠ домики ≠ кассы ≠ сотрудники ≠ продажи
```

---

## 2. Структура складов

### Центральный склад

**Бишкек — центральный.** Туда приходит всё новое:

```
Покупка → Центральный склад Бишкек → распределение по объектам
```

Из него товар передаётся на: Площадь, Азия Молл, Скай Парк, Кой Таш.

### Самостоятельные склады

- **Ош** — свои закупки, свои остатки, свой контур.
- **Лермонтова** — реквизит, техника, оборудование.
- **Прохладное** — домики и оборудование ярмарки.

### Текущий список (8 складов)

| № | Склад | Код |
|---|-------|-----|
| 1 | Бишкек — центральный | `BISHKEK_CENTRAL` |
| 2 | Кой Таш | `KOI_TASH` |
| 3 | Площадь | `PLOSHAD` |
| 4 | Азия Молл | `ASIA_MALL` |
| 5 | Скай Парк | `SKY_PARK` |
| 6 | Ош | `OSH` |
| 7 | Лермонтова | `LERMONTOVA` |
| 8 | Прохладное | `PROKHLADNOE` |

→ `lib/db/src/schema/locations.ts` (`DEFAULT_LOCATIONS`), автосев при первом
запросе `GET /api/locations`.

---

## 3. Логика движения товара

```
Закупка → Бишкек-центральный → Перемещение → Локальный склад
        → Объект / домик → Продажа / использование → Отчётность
```

**Ключевое правило:** центральный склад не уменьшается от того, что кто-то где-то
продал товар. Он уменьшается только при подтверждённом перемещении.

Отдельный контур:

```
ОШ
├── свой склад
├── свои закупки
└── свои остатки
```

---

## 4. Роли и права

| Роль | Код | Ранг | Доступ |
|------|-----|------|--------|
| Завхоз / главный администратор | `admin` | 4 | всё, все склады |
| Управляющий | `manager` | 3 | без отчётов, аудита, настроек |
| Бухгалтер | `accountant` | 2 | + списания, без категорий и сотрудников |
| Сотрудник склада | `warehouse` | 1 | только свой склад |

Иерархия — `artifacts/api-server/src/middleware/rbac.ts`.
Разграничение по складу — `artifacts/api-server/src/lib/warehouseScope.ts`.

Сотрудник Азия Молл не должен видеть внутреннюю информацию Оша или центрального склада.

---

## 5. Модули системы

### Приход товара
`Поступление → выбор склада → товар → количество`
Пример: купили 100 стульев → Бишкек-центральный → +100.

### Перемещения (двухэтапные)
1. Завхоз создаёт перемещение;
2. товар списывается с источника в той же транзакции;
3. статус `pending`;
4. склад-получатель принимает (`accept`) или отклоняет (`reject`);
5. только после подтверждения товар появляется на складе назначения;
6. при отклонении остаток возвращается на склад-источник;
7. в истории: кто создал, кто принял, откуда, куда, какой товар, количество, даты.

### Товары и категории
Категории → товары → единицы измерения → остатки → складские остатки → история движения.
Примеры: *Мебель* (стол, стул, диван), *Реквизит* (декорация, костюм, свет).

### Списание
`Списание → склад → товар → количество → причина → подтверждение`
Причины: повреждение, поломка, потеря, расход, утилизация, другое.

### Инвентаризация
Фактический пересчёт по конкретному складу:
```
По системе: 50 / Фактически: 47 / Разница: −3
```
После подтверждения — сохранение истории.

### Аренда
Выдача и возврат вещей и оборудования: кому, сколько, до какой даты, статус.

### Сотрудники
Имя, email, роль, склад, статус, Telegram, история действий.
Завхоз назначает сотруднику конкретный склад.

### Журнал аудита
Система должна отвечать на вопрос «кто что сделал»:
```
Арсен · 21.09.2026 18:43 · создал перемещение · Бишкек → Азия Молл · Стулья × 20
```
Нужно, чтобы нельзя было сказать «я этого не делал».

### Отчёты и аналитика
- **по складам:** остатки, движение, приход, расход, списания;
- **по товарам:** купили / переместили / списали / осталось;
- **по сотрудникам:** кто какие операции выполнял;
- **по периодам:** день, неделя, месяц, произвольный период.

---

## 6. Ярмарка «Северное сияние» — отдельный уровень

**5 площадок × 6–8 домиков = 30–40 домиков.**

Кассы — отдельный слой, а не ещё один склад:

```
Площадка → Домик → Касса → Кассир → Смена → Продажи → Отчёт
```

### Кассовая система
Открытие смены → кассир → товары → количество → цена → скидка → итог →
наличные/безнал → закрытие смены → возвраты → отчёт.
Завхоз/администратор видит всю картину.

### Связь склада и ярмарки

```
             ЦЕНТРАЛЬНЫЙ СКЛАД
                    │
          ┌─────────┼─────────┐
          ↓         ↓         ↓
      Площадка    Азия     Скай Парк
          │
       Домики
          │
       Кассы
          │
      Продажи
          │
       Отчёты
```

---

## ✅ Что уже сделано

Сверено с кодом на 21.09.2026.

### Инфраструктура
- [x] pnpm-монорепозиторий: `artifacts/` (api-server, m-sklad, mockup-sandbox) + `lib/` (db, api-spec, api-zod, api-client-react, object-storage-web)
- [x] PostgreSQL + Drizzle ORM, 14 таблиц
- [x] авторизация Clerk (`@clerk/express`), русская локализация форм входа
- [x] Render-сервис `luna` + база `luna-db` (`render.yaml`)
- [x] автоприменение схемы БД перед стартом (`prestart: drizzle-kit push`)
- [x] CI GitHub Actions: typecheck + build
- [x] Express отдаёт собранный фронтенд статикой (`app.ts`)

### Склады и доступы
- [x] 8 складов, центральный Бишкек, автосев `DEFAULT_LOCATIONS`
- [x] 4 роли с иерархией рангов (`rbac.ts`)
- [x] привязка сотрудника к складу (`users.locationId`)
- [x] ограничение доступа по складу (`warehouseScope.ts`)
- [x] завхоз (`admin`) видит все объекты
- [x] первый зарегистрированный аккаунт получает роль `admin`
- [x] редактирование/отключение склада администратором (`PATCH /api/locations/:id`)

### Операции
- [x] поступления с выбором склада
- [x] перемещения между складами: создание → `pending` → `accept` / `reject`
- [x] перемещения в транзакции, с проверкой достаточности остатка
- [x] защита от двойного подтверждения (условный `UPDATE ... WHERE status = 'pending'`)
- [x] возврат остатка на источник при отклонении
- [x] складские остатки (`warehouse_stock`) + синхронизация суммарного остатка товара
- [x] категории, товары, единицы измерения
- [x] списания с причиной, транзакциями и защитой от ухода в минус
- [x] инвентаризация (`inventory_audits` + `audit_items`)
- [x] аренда
- [x] сотрудники
- [x] отчёты + выгрузка Excel (`xlsx`), печать в PDF
- [x] журнал аудита (`logAudit` в основных операциях, пагинация на чтении)
- [x] Telegram-бот (`telegraf`) и загрузка фото в объектное хранилище

### Интерфейс
- [x] 15 страниц (dashboard, items, transfers, categories, receipts, write-offs,
      inventory-audits, rentals, staff, reports, audit-log, settings, onboarding,
      landing, 404)
- [x] React + Vite + Radix UI/shadcn, мобильная вёрстка
- [x] ролевые маршруты (`RoleProtectedRoute` с `minRole`)
- [x] брендинг «Северное сияние» (sidebar, onboarding, `index.html`)
- [x] регистрация через email, базовая логика назначения роли/склада

Последний коммит фундамента: `421b6eae` — `fix: apply database schema before Luna starts`.

---

## ⚠️ Найденные проблемы

Обнаружены при сверке плана с кодом. Это не «хотелки», а реальные дефекты.

### Высокий приоритет

1. **Аналитика не ограничена складом.** `routes/analytics.ts` требует авторизацию,
   но не вызывает `getWarehouseScope`. Сотрудник Азия Молл через дашборд видит
   цифры по всем складам, включая Ош и центральный. Это прямое нарушение
   требования из раздела 4.

2. **`drizzle-kit push` в `prestart` может зависнуть.** При изменениях, которые
   drizzle считает потенциально разрушительными, `push` уходит в интерактивный
   запрос. На Render это приведёт к зависанию или падению деплоя. Есть готовый
   скрипт `push-force`, но он не используется. Это вероятная причина 500-х:
   таблицы не создаются → запросы к `users` падают.

3. **Дамп боевой базы в репозитории.** `backup_database.sql` (70 КБ) содержит
   таблицы `users`, `staff`, `audit_log` — реальные email и телефоны (~144
   вхождения). Персональные данные в истории коммитов.

### Средний приоритет

4. **Дублирование логики «первый пользователь = admin».** Одна копия в
   `routes/auth.ts`, вторая в `middleware/rbac.ts`, причём поведение при ошибке
   подсчёта различается (`count: 0` против `count: 1`). При гонке двух
   одновременных первых входов возможна выдача `admin` не тому.

5. **Захардкоженный список складов в UI.** `components/layout.tsx:9` содержит
   7 названий и **не содержит «Бишкек — центральный»**. Сотрудник, назначенный на
   центральный склад, видит в боковой панели «Склад не назначен». Список нужно
   брать из `GET /api/locations`.

6. **Смешанный брендинг.** `pages/reports.tsx` и `pages/landing.tsx` всё ещё
   показывают «Luna-Sklad», остальное приложение — «Северное сияние».

7. **Нет пагинации** на списках `items`, `receipts`, `write-offs`, `transfers`,
   `rentals`, `staff`. Пагинация есть только в `audit-log` и частично в аналитике.
   На объёмах ярмарки это станет проблемой.

8. **Нет ни одного теста.** CI проверяет только typecheck и сборку. Складская
   логика с транзакциями и остатками не покрыта.

### Инфраструктура репозитория

9. **~570 МБ мусора в git.** `project_part_aa`…`ag` (~283 МБ) + `project.zip`
   (LFS, 290 МБ) + каталог `СКАЧАТЬ_КОД/` — дублирующая копия `artifacts/` и
   `lib/`. Папка `.git` весит 259 МБ.

10. **Render free plan.** Сервис засыпает при простое (первый запрос после сна —
    до минуты), бесплатная база имеет ограниченный срок жизни. Для боевой
    эксплуатации нужен платный план.

---

## 🗺️ Backlog

### Этап 1 — довести production до стабильного состояния

Сначала — 500-е ошибки API:

```text
/api/auth/me  → 500
/api/locations → 500
/api/transfers → 500
```

Ошибка возникает при запросе к `users` — почти наверняка таблицы не созданы.

- [ ] заменить `prestart` на неинтерактивный вариант (`push --force` или
      полноценные миграции `drizzle-kit migrate`)
- [ ] проверить новый Render deploy
- [ ] убедиться, что таблицы создались
- [ ] `/api/auth/me` работает
- [ ] `/api/locations` работает (и сеет 8 складов)
- [ ] `/api/transfers` работает
- [ ] пользователь нормально создаётся
- [ ] первый пользователь получает роль администратора
- [ ] проверить, что `CLERK_SECRET_KEY` задан в окружении Render
      (без него `auth/me` не получит email из Clerk)

### Этап 2 — полная проверка системы

Новые функции не добавляем. Проходим всё как настоящий пользователь.

**Авторизация:** регистрация · вход · выход · повторный вход
- [ ] регистрация
- [ ] вход
- [ ] выход
- [ ] повторный вход

**Главная**
- [ ] Dashboard
- [ ] аналитика

**Склады**
- [ ] 8 складов на месте
- [ ] остатки
- [ ] переключение
- [ ] права доступа

**Категории**
- [ ] создание
- [ ] редактирование
- [ ] удаление
- [ ] отображение

**Товары**
- [ ] создание
- [ ] редактирование
- [ ] остатки

**Поступления**
- [ ] приход
- [ ] выбор склада
- [ ] изменение остатка

**Перемещения**
- [ ] создание
- [ ] ожидание
- [ ] подтверждение
- [ ] изменение остатков

**Списание**
- [ ] создание
- [ ] причина
- [ ] изменение остатка

**Инвентаризация**
- [ ] пересчёт
- [ ] расхождения
- [ ] подтверждение

**Сотрудники**
- [ ] создание
- [ ] роль
- [ ] склад
- [ ] доступ

**Аудит**
- [ ] все действия записываются

### Этап 3 — production-ready складская система

- [ ] закрыть аналитику складским scope (проблема №1)
- [ ] убрать захардкоженный список складов из UI (проблема №5)
- [ ] свести логику «первый пользователь = admin» в одно место (проблема №4)
- [ ] единый брендинг «Северное сияние» (проблема №6)
- [ ] защита API и сквозная проверка ролей на всех маршрутах
- [ ] единая обработка ошибок
- [ ] валидация входных данных через `@workspace/api-zod`
- [ ] защита от отрицательных остатков во всех модулях
- [ ] транзакции для всех складских операций (сейчас их нет в `rentals`, `staff`)
- [ ] защита от двойного подтверждения везде, где есть статусы
- [ ] полная история изменений
- [ ] пагинация больших таблиц (проблема №7)
- [ ] поиск
- [ ] фильтры
- [ ] экспорт отчётов
- [ ] тесты на складскую логику (проблема №8)
- [ ] убрать `backup_database.sql` и очистить историю (проблема №3)
- [ ] выкинуть `project_part_*`, `project.zip`, `СКАЧАТЬ_КОД/` (проблема №9)

### Этап 4 — ярмарка

**Площадки**
- [ ] модель `venues`, 5 площадок

**Домики**
- [ ] модель `houses`, 30–40 домиков
- [ ] привязка домика к площадке

**Кассы**
- [ ] модель `registers`, 30–40 касс
- [ ] касса → домик
- [ ] кассир → касса

**Смены**
- [ ] открытие
- [ ] работа
- [ ] закрытие
- [ ] итог смены

**Продажи**
- [ ] товары
- [ ] цены
- [ ] количество
- [ ] оплата (наличные / безнал)
- [ ] возвраты

### Этап 5 — связь склада и ярмарки

- [ ] перемещение со склада в домик
- [ ] остаток домика как источник продаж
- [ ] сквозная отчётность «склад → домик → касса → продажа»
- [ ] отдельный контур Оша не смешивается с ярмаркой

### Этап 6 — автоматизация

- [ ] Telegram-уведомления
- [ ] уведомление о низком остатке
- [ ] уведомление о новом перемещении
- [ ] уведомление о подтверждении перемещения
- [ ] автоматические ежедневные/недельные отчёты
- [ ] резервные копии БД (вне репозитория)
- [ ] мониторинг Render
- [ ] перевод Render на платный план

---

## Где мы сейчас

| | |
|---|---|
| Фундамент | практически собран |
| Production | запущен, идёт исправление API/БД |
| Следующий шаг | добиться, чтобы все разделы работали с production `luna-db` |
| После этого | полный тест всех модулей → исправления → кассовая/ярмарочная часть |

**Правило:** все изменения попадают в GitHub `bxbyarchi/luna`, а не остаются
только на Render или локально.
